<Window x:Class="DesktopMessenger.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="356" Width="535" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid>
        <TreeView x:Name="settingsTreeView" HorizontalAlignment="Left" Width="187">
            <TreeViewItem x:Name="AccountsTreeViewItem" Header="Accounts" IsExpanded="True" ItemsSource="{Binding Accounts, UpdateSourceTrigger=PropertyChanged}"/>
            <TreeViewItem x:Name="GeneraTreeViewItem" Header="General">
            </TreeViewItem>
        </TreeView>
        <ComboBox x:Name="accountTypeBox" HorizontalAlignment="Left" Margin="343,10,0,0" VerticalAlignment="Top" Width="120" SelectedIndex="0">
            <ComboBoxItem Tag="facebook" Content="Facebook"/>
        </ComboBox>
        <Label Content="Protocol:" HorizontalAlignment="Left" Margin="229,6,0,0" VerticalAlignment="Top"/>
        <Label Content="Username:" HorizontalAlignment="Left" Margin="229,34,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="UsernameBox" HorizontalAlignment="Left" Height="23" Margin="343,37,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <PasswordBox x:Name="PassBox" HorizontalAlignment="Left" Height="23" Margin="343,65,0,0" VerticalAlignment="Top" Width="120" />
        <Label Content="Password:" HorizontalAlignment="Left" Margin="229,61,0,0" VerticalAlignment="Top"/>
        <Button x:Name="AddBtn" Content="Add Acount" HorizontalAlignment="Left" Margin="280,120,0,0" 
                VerticalAlignment="Top" Width="75" IsDefault="True" Command="{Binding AddAccountCommand}" Click="AddBtn_Click"/>
        <Button x:Name="Cancel" Content="Cancel" HorizontalAlignment="Left" Margin="388,120,0,0" 
                VerticalAlignment="Top" Width="75" Command="{Binding CancelCommand}" IsCancel="True"/>

    </Grid>
</Window>
